import { ShapeDrawerDirective } from './shape-drawer.directive';

describe('RectDrawerDirective', () => {
  it('should create an instance', () => {
    // const directive = new ShapeDrawerDirective();
    // expect(directive).toBeTruthy();
  });
});
